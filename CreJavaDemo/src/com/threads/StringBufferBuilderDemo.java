package com.threads;

public class StringBufferBuilderDemo {

	public static void main(String[] args) {
		StringBuffer b1 = new StringBuffer();// slower + synchronized
		StringBuilder b2 = new StringBuilder(); // faster + not synchronized
		

	}

}
